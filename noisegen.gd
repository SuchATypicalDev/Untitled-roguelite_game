extends MeshInstance3D

# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	pass
	#var noise = Image.create(1024,1024,true,Image.FORMAT_RGBA8)
	#for i in range(1024):
		#for j in range(1024):
			#var rand = randi_range(0,255)
			#var col = Color8(rand,rand,rand,255)
			#noise.set_pixel(i,j,col)
	#noise.save_png("res://noise.png")
	
